- id: stages
  name: '../../../../../stage/dist/preset.js'
  config:
    stages:
      board1:
        machine: gomoku
- id: performance
  name: '../../../../dist/preset.js'
  config:
    characters:
      black:
        systemPrompt: You are the black Gomoku test Character.
        tools:
          - factory: gomoku-place-stone
            params: { stage: board1, color: black }
- id: first-director
  name: '../../../../../theater-gomoku/dist/director.js'
  config: { stage: board1, instruction: Make one legal move. }
- id: second-director
  name: '../../../../../theater-gomoku/dist/director.js'
  config: { stage: board1, instruction: Make one legal move. }
