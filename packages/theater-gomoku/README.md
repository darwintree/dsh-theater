# @darwintree/dsh-theater-gomoku

The first domain package for `@darwintree/dsh-theater`.

It contributes the `gomoku/move` Theater fact, a pure replay fold, board and turn rendering, and a character-scoped `place_stone` DSH tool.
